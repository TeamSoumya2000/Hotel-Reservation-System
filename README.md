# Hotel-Reservation-System
Hotel Reservation System is a SQL-based database project that manages room bookings, customer details, payments, and availability. It uses relational tables with primary and foreign keys to maintain data integrity. The system supports booking management, room availability checks, and revenue reporting using SQL queries and joins.
